import { sampleURLs } from "../../samples/sampleURLs";

export const OneShotPresets: { [key: string]: URL[] } = {
  zenBeach: [
    sampleURLs.transposers.celeste.url,
    sampleURLs.transposers.spacePiano.url,
  ],
};
